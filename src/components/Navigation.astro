---
import { Image } from 'astro:assets'
import tedxLogoBlack from '../assets/logo-black.webp'
import tedxLogoWhite from '../assets/logo-white.webp'

const navItems = [
  { label: 'Inicio', id: 'hero' },
  { label: 'Evento', id: 'evento' },
  { label: 'Speakers', id: 'speakers' },
  { label: 'Agenda', id: 'agenda' },
  { label: 'Entradas', id: 'tickets' },
  { label: 'FAQ', id: 'faq' },
  { label: 'Equipo', id: 'equipo' },
  { label: 'Sponsors', id: 'sponsors' },
  { label: 'Contacto', id: 'contacto' },
]
---

<nav class="nav-container">
  <div class="nav-content">
    <a href="#hero" class="logo">
      <Image src={tedxLogoBlack} alt="TEDx Logo" width={200} id="nav-logo-light" />
      <Image src={tedxLogoWhite} alt="TEDx Logo" width={200} id="nav-logo-dark" style="display: none;" />
    </a>

    <!-- Desktop Menu -->
    <ul class="nav-menu">
      {
        navItems.map((item) => (
          <li>
            <a href={`#${item.id}`} class="nav-link">
              {item.label}
            </a>
          </li>
        ))
      }
    </ul>

    <!-- Theme Toggle Button -->
    <button class="theme-toggle" id="theme-toggle" aria-label="Toggle theme">
      <svg id="sun-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <circle cx="12" cy="12" r="5"></circle>
        <line x1="12" y1="1" x2="12" y2="3"></line>
        <line x1="12" y1="21" x2="12" y2="23"></line>
        <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
        <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
        <line x1="1" y1="12" x2="3" y2="12"></line>
        <line x1="21" y1="12" x2="23" y2="12"></line>
        <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
        <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
      </svg>
      <svg
        id="moon-icon"
        width="24"
        height="24"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        style="display: none;"
      >
        <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
      </svg>
    </button>

    <!-- Mobile Menu Button -->
    <button class="nav-toggle" id="nav-toggle" aria-label="Toggle menu">
      <svg id="menu-icon" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <line x1="3" y1="12" x2="21" y2="12"></line>
        <line x1="3" y1="6" x2="21" y2="6"></line>
        <line x1="3" y1="18" x2="21" y2="18"></line>
      </svg>
      <svg
        id="close-icon"
        width="28"
        height="28"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        style="display: none;"
      >
        <line x1="18" y1="6" x2="6" y2="18"></line>
        <line x1="6" y1="6" x2="18" y2="18"></line>
      </svg>
    </button>

    <!-- Mobile Menu -->
    <div class="nav-mobile-menu" id="mobile-menu" style="display: none;">
      <ul>
        {
          navItems.map((item) => (
            <li>
              <button data-scroll-to={item.id} class="nav-mobile-link">
                {item.label}
              </button>
            </li>
          ))
        }
      </ul>
    </div>
  </div>
</nav>

<script>
  // Theme toggle functionality
  const themeToggle = document.getElementById('theme-toggle')
  const sunIcon = document.getElementById('sun-icon')
  const moonIcon = document.getElementById('moon-icon')
  const navLogoLight = document.getElementById('nav-logo-light')
  const navLogoDark = document.getElementById('nav-logo-dark')
  const html = document.documentElement

  // Function to get system theme preference
  function getSystemTheme(): string {
    if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
      return 'dark'
    }
    return 'light'
  }

  // Function to update theme UI
  function updateThemeUI(theme: string) {
    // Update icons
    if (sunIcon && moonIcon) {
      if (theme === 'dark') {
        sunIcon.style.display = 'none'
        moonIcon.style.display = 'block'
      } else {
        sunIcon.style.display = 'block'
        moonIcon.style.display = 'none'
      }
    }

    // Update navigation logos
    if (navLogoLight && navLogoDark) {
      if (theme === 'dark') {
        navLogoLight.style.display = 'none'
        navLogoDark.style.display = 'block'
      } else {
        navLogoLight.style.display = 'block'
        navLogoDark.style.display = 'none'
      }
    }

    // Update hero logos
    const heroLogoLight = document.querySelector('.hero .logo-light') as HTMLElement
    const heroLogoDark = document.querySelector('.hero .logo-dark') as HTMLElement
    if (heroLogoLight && heroLogoDark) {
      if (theme === 'dark') {
        heroLogoLight.style.display = 'none'
        heroLogoDark.style.display = 'block'
      } else {
        heroLogoLight.style.display = 'block'
        heroLogoDark.style.display = 'none'
      }
    }

    // Update contact logos
    const contactLogoLight = document.querySelector('.contact-content .logo-light') as HTMLElement
    const contactLogoDark = document.querySelector('.contact-content .logo-dark') as HTMLElement
    if (contactLogoLight && contactLogoDark) {
      if (theme === 'dark') {
        contactLogoLight.style.display = 'none'
        contactLogoDark.style.display = 'block'
      } else {
        contactLogoLight.style.display = 'block'
        contactLogoDark.style.display = 'none'
      }
    }
  }

  // Get current theme (already set by inline script in Layout)
  const currentTheme = html.getAttribute('data-theme') || getSystemTheme()

  // Update UI to match current theme
  updateThemeUI(currentTheme)

  // Listen for system theme changes (when user changes OS theme)
  if (window.matchMedia) {
    window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', (e) => {
      // Only update if user hasn't manually set a preference
      if (!localStorage.getItem('theme')) {
        const newTheme = e.matches ? 'dark' : 'light'
        html.setAttribute('data-theme', newTheme)
        updateThemeUI(newTheme)
      }
    })
  }

  if (themeToggle) {
    themeToggle.addEventListener('click', () => {
      const currentTheme = html.getAttribute('data-theme')
      const newTheme = currentTheme === 'light' ? 'dark' : 'light'

      html.setAttribute('data-theme', newTheme)
      localStorage.setItem('theme', newTheme)
      updateThemeUI(newTheme)
    })
  }

  // Mobile menu toggle
  const navToggle = document.getElementById('nav-toggle')
  const mobileMenu = document.getElementById('mobile-menu')
  const menuIcon = document.getElementById('menu-icon')
  const closeIcon = document.getElementById('close-icon')

  if (navToggle && mobileMenu && menuIcon && closeIcon) {
    navToggle.addEventListener('click', () => {
      const isOpen = mobileMenu.style.display !== 'none'
      mobileMenu.style.display = isOpen ? 'none' : 'block'
      menuIcon.style.display = isOpen ? 'block' : 'none'
      closeIcon.style.display = isOpen ? 'none' : 'block'
    })
  }

  // Mobile menu links - close menu on click and scroll
  const mobileLinks = document.querySelectorAll('.nav-mobile-link')
  mobileLinks.forEach((link) => {
    link.addEventListener('click', (e) => {
      const targetId = (e.currentTarget as HTMLElement).dataset.scrollTo
      const element = document.getElementById(targetId || '')
      if (element) {
        element.scrollIntoView({ behavior: 'smooth' })
        // Close mobile menu
        if (mobileMenu && menuIcon && closeIcon) {
          mobileMenu.style.display = 'none'
          menuIcon.style.display = 'block'
          closeIcon.style.display = 'none'
        }
      }
    })
  })
</script>

<style>
  .nav-container {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    background-color: var(--bg-nav);
    box-shadow: 0 2px 10px var(--shadow-color);
    z-index: 1000;
    transition:
      background-color 0.3s ease,
      box-shadow 0.3s ease;
  }

  .nav-content {
    max-width: 1400px;
    margin: 0 auto;
    padding: 1rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
  }

  .nav-menu {
    display: flex;
    list-style: none;
    gap: 2rem;
    margin: 0;
    padding: 0;
  }

  .nav-link {
    color: var(--text-primary);
    cursor: pointer;
    font-size: 1rem;
    font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    transition: color 0.3s ease;
    padding: 0.5rem 0;
    position: relative;
    text-decoration: none;
  }

  .nav-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background-color: var(--tedx-red);
    transition: width 0.3s ease;
  }

  .nav-link:hover {
    color: var(--tedx-red);
  }

  .nav-link:hover::after {
    width: 100%;
  }

  .theme-toggle {
    background: none;
    border: none;
    cursor: pointer;
    color: var(--text-primary);
    padding: 0.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    transition: all 0.3s ease;
  }

  .theme-toggle:hover {
    background-color: var(--bg-secondary);
    transform: rotate(20deg);
  }

  .nav-toggle {
    display: none;
    background: none;
    border: none;
    cursor: pointer;
    color: var(--text-primary);
    padding: 0.5rem;
  }

  .nav-mobile-menu {
    display: none;
  }

  .logo {
    text-decoration: none;
    line-height: 1;
    cursor: pointer;
    position: relative;
  }

  /* Mobile Responsive Styles */
  @media (max-width: 968px) {
    .nav-content {
      padding: 1rem 1.5rem;
    }

    .nav-menu {
      display: none;
    }

    .nav-toggle {
      display: block;
    }

    .nav-mobile-menu {
      display: block;
      position: absolute;
      top: 100%;
      left: 0;
      right: 0;
      background-color: var(--bg-nav);
      box-shadow: 0 5px 15px var(--shadow-color);
      animation: slideDown 0.3s ease;
    }

    @keyframes slideDown {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .nav-mobile-menu ul {
      list-style: none;
      padding: 1rem 0;
      margin: 0;
    }

    .nav-mobile-link {
      display: block;
      width: 100%;
      padding: 1rem 2rem;
      background: none;
      border: none;
      text-align: left;
      color: var(--text-primary);
      font-size: 1.1rem;
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .nav-mobile-link:hover {
      background-color: var(--bg-secondary);
      color: var(--tedx-red);
      padding-left: 2.5rem;
    }
  }

  @media (max-width: 640px) {
    .nav-content {
      padding: 0.8rem 1rem;
    }

    .logo img {
      max-width: 150px;
      height: auto;
    }
  }
</style>
